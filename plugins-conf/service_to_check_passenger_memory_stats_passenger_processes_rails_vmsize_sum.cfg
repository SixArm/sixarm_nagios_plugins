define service{
  use                             generic-service
  host_name                       localhost
  service_description             passenger memory stats - passenger processes - rails vmsize sum
  check_command                   check_passenger_memory_stats_passenger_processes_rails_vmsize_sum
}

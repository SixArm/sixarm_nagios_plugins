define service{
  use                             generic-service
  host_name                       localhost
  service_description             passenger memory stats - apache processes - total private dirty
  check_command                   check_passenger_memory_stats_apache_processes_total_private_dirty
}
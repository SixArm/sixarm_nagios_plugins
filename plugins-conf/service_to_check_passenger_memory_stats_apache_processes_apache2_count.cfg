define service{
  use                             generic-service
  host_name                       localhost
  service_description             passenger memory stats - apache processes - apache2 count
  check_command                   check_passenger_memory_stats_apache_processes_apache2_count
}

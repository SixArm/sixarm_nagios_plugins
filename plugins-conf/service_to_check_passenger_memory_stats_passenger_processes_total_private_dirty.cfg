define service{
  use                             generic-service
  host_name                       localhost
  service_description             passenger memory stats - passenger processes - total private dirty
  check_command                   check_passenger_memory_stats_passenger_processes_total_private_dirty
}